import styled from "styled-components";

export const Container = styled.div`
    position: fixed; 
    inset: 0; 
    height: 100vh; 
    width: 100%; 
    background-color: #1d1b1b; 
    z-index: 30;
`